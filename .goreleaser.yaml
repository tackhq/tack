# GoReleaser configuration for Tack
# Documentation: https://goreleaser.com

version: 2

project_name: tack

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: tack
    binary: tack
    main: ./cmd/tack
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md
      - LICENSE*
      - docs/*

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug Fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Other
      order: 999

release:
  github:
    owner: tackhq
    name: tack
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Tack {{ .Version }}

    Configuration management and system bootstrapping tool.
  footer: |
    ## Installation

    ### Homebrew (macOS/Linux)
    ```bash
    brew install tackhq/tap/tack
    ```

    ### Direct Download
    Download the appropriate binary for your platform from the assets below.

    ### Build from Source
    ```bash
    go install github.com/tackhq/tack/cmd/tack@{{ .Tag }}
    ```

# Homebrew Tap
brews:
  - name: tack
    repository:
      owner: tackhq
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/tackhq/tack"
    description: "Configuration management and system bootstrapping tool inspired by Ansible"
    license: "MIT"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    dependencies:
      - name: go
        type: optional
    test: |
      system "#{bin}/tack", "--version"
    install: |
      bin.install "tack"

# Announce release (optional)
announce:
  skip: true
